#include<stdio.h>
#include<string.h>
#include "main.h"

/**
 *_strlen_recursion -  returns the length of string
 *
 * @s: character to work with.
 *
 * @i: length of string
 * Returns: i
 */

int _strlen_recursion(char *s)
{
	int i;

	i = strlen(s);

	return (i);
}
